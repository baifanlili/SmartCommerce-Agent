# SmartCommerce-Agent 版本路线图

本文描述 SmartCommerce-Agent 的公开产品与技术演进计划。项目定位是“面向电商平台的 Multi-Agent 智能购物能力平台”，面向用户的 MVP 客户端采用移动端优先的跨端技术路线。版本目标同时覆盖产品能力和架构能力，每个设计书中的重要模块都必须有明确的实现版本和验收标准。

## 路线图原则

- 先完成可运行的用户闭环，再逐步引入复杂的 Agent 基础设施。
- 让大模型负责理解、规划和解释，让确定性服务负责商品事实、库存和订单执行。
- 将客户端视为可替换的接入层：面向用户的 MVP 官方客户端采用 Taro + React + TypeScript，适配 H5 和各类小程序构建目标；现有 Vite Web 端仅作为技术验证与接口演示端。核心 Agent 能力通过 API、SSE 或 WebSocket 对外提供。
- 每个版本都必须有可演示、可测试的验收结果，未完成的能力不提前包装成企业级能力。
- 交易、支付和库存等强一致业务在独立的 Java 业务服务中实现，Agent 通过受控工具调用这些能力。
- 保留 Mock 模式，保证没有模型密钥时仍然可以启动项目和运行回归测试。
- Java 负责统一登录认证、用户/角色/租户和交易业务权限；Python Agent 负责对话历史、长期记忆、用户偏好和语义记忆。
- Python Agent 不重复实现登录体系，但必须校验 Java 或宿主平台签发的身份凭证，并在 Agent 内部区分普通用户和管理员。
- Agent 记忆按 `user_id`、`tenant_id` 和 `session_id` 隔离；客户端不能直接决定这些身份字段，不能通过修改参数访问他人记忆。
- 从 `v0.1.0` 起按生产级约束设计：API 无状态、依赖可替换、配置外置、接口可观测、错误可恢复，并且每个版本都有对应压测场景和性能记录。
- 压测优先使用 Mock LLM 或可控 Provider，分别测量 API、Agent 编排、记忆读写、队列 Worker 和真实模型调用，避免把第三方模型波动误判为自身服务性能。
- 开源数据集分阶段接入：`v0.1.0` 使用仓库内小型、可追踪的样例数据保证回归稳定；`v0.3.0` 接入经过许可证核验、清洗、去重和版本锁定的开源商品/评价数据集；生产环境不直接依赖临时下载地址，而是使用经过审核的内部数据快照和可重建的数据管道。
- 数据集接入必须记录来源、许可证、下载时间、版本或 Commit、字段映射、清洗规则、脱敏结果和校验摘要；原始数据、处理后数据和向量索引分层保存，避免把大文件和可能包含个人信息的原始内容提交到 Git。

## 生产级基线与压测原则

“从第一天按生产级设计”不等于第一天就拆成很多微服务，而是从第一版代码开始遵守生产约束：

- API 服务不在进程内保存用户会话、长期记忆或任务状态，任何副本都可以处理任意请求。
- 用户身份、租户、会话、记忆和请求链路使用稳定 ID 关联，接口必须校验归属和权限。
- 超时、有限重试、熔断、降级、幂等和错误码从接口设计阶段确定，不能等线上故障后再补。
- 日志、指标和 Trace 从第一条主链路接入，至少能够定位请求、Agent、LLM、Memory、Tool 和外部依赖的耗时与失败点。
- 通过 Docker Compose 完成本地复现，通过多副本容器或 Kubernetes 验证横向扩容；部署拓扑可以演进，服务契约不能频繁推翻。
- 每个版本都维护压测脚本、测试数据、并发模型、环境说明和结果记录；性能优化必须有前后对比。

压测分层：

| 压测层 | 主要目标 | 推荐方式 |
|---|---|---|
| API 基准压测 | 吞吐、P95/P99、错误率和连接稳定性 | Mock LLM + 固定商品数据 |
| Agent 流程压测 | Supervisor、意图识别、路由和结果汇总开销 | 固定工作流 + 可控 Provider |
| 记忆压测 | 会话读写、摘要、向量检索和数据隔离 | Redis/持久化存储/向量库组合 |
| 异步任务压测 | 入队速度、消费速度、队列堆积和恢复能力 | 队列 + 多类 Worker |
| 依赖与故障压测 | 超时、限流、熔断、降级和重试是否生效 | 故障注入 + 依赖 Mock |
| 真实模型压测 | Provider QPS、Token、成本和端到端延迟 | 受控小规模测试，遵守供应商限制 |

压测结果至少记录：并发数、持续时间、请求成功率、吞吐量、P50/P95/P99 延迟、超时率、依赖错误率、Token 消耗和资源使用率。具体 SLO 数值在获得基线后确定，不在没有实测数据时虚构指标。

## 生产级架构与部署路线

架构演进遵循“模块化单体起步、无状态 API、外置状态、异步 Worker、多副本扩容、故障演练和自动发布”的顺序。第一天就遵守生产约束，但不在没有压力依据时把每个 Agent 拆成独立微服务。

| 阶段 | 运行架构 | 部署与验证重点 |
|---|---|---|
| `v0.1.0` | FastAPI 模块化 Agent 服务 + Redis，API 无状态，Mock LLM 可替换 | Docker Compose 一键启动；健康检查、请求 ID、结构化错误、基础 API 压测 |
| `v0.2.0` | 统一 LLM Provider/Gateway 契约，模型依赖外置配置 | Compose 环境变量注入；模型超时、重试、降级和 Provider 契约测试 |
| `v0.3.0` | Taro 接入层 + Python Agent + 数据导入/RAG 管道 | H5 容器部署；数据快照、索引构建、数据质量和 RAG 离线评估 |
| `v0.4.0` | Supervisor、Intent、Planner、Router、Aggregator 和 Agent Registry 组成的可编排运行时 | 单服务内先验证状态图、工具白名单和任务恢复；固定工作流压测 |
| `v0.5.0` | Agent API 多副本 + Redis 外置状态 + Queue + Worker Pool | 负载均衡、多副本、队列堆积、幂等、重试、Worker 故障和横向扩容压测 |
| `v0.6.0` | Python Agent 通过 MCP Gateway 接入 Java 商品、库存、订单等业务服务 | Java 无状态多副本、网关负载均衡、缓存、数据库和消息队列高并发基线；业务权限、交易幂等和跨服务链路验证 |
| `v0.7.0` | OpenTelemetry、Prometheus/Grafana、评估与管理员运营能力 | dev 环境观测闭环、数据脱敏、质量门禁和成本统计 |
| `v0.8.0` | API Gateway、限流熔断、自动扩容、备份恢复和发布控制 | dev → uat → prod 逐级晋级；灰度、滚动发布、回滚、故障注入和长稳压测 |
| `v1.0.0` | 完整生产拓扑，按实际容量选择容器编排和托管依赖 | 生产 SLO、容量模型、灾备演练、安全审计和同镜像发布闭环 |

部署环境统一使用同一份不可变镜像，通过配置注入环境差异：本地使用 Docker Compose，集成环境可使用多副本容器，生产环境再根据容量和团队运维能力选择 Kubernetes 或云厂商容器平台。数据库、Redis、消息队列、向量库和模型密钥不写入镜像，生产凭据使用 Secret Manager 或等价的密钥管理能力。分布式能力按“先明确边界、再拆分运行时、最后横向扩容”的顺序建设，不为了架构名词提前引入没有业务压力支撑的中间件。

## 必须提前锁定的架构边界

这些规则从早期版本开始生效，后续版本只能扩展实现，不能随意改变职责归属：

- Java 业务层拥有用户、商品、购物车、库存、订单和支付状态；Python Agent 平台拥有会话历史、长期记忆、用户偏好、画像和 Agent 运行上下文。
- Redis 只保存缓存、短期状态、幂等键和分布式协调数据，不能作为订单或库存的最终事实；核心业务事实由 MySQL 等持久化存储保存。
- 每个 Java 服务拥有自己的表和写权限。初期可以共用 MySQL 实例，但禁止跨服务直接读写其他服务的业务表，为未来 Schema 或数据库拆分保留空间。
- 对外 REST 接口统一使用 `/api/v1`，统一错误码、分页、排序、`request_id`、`trace_id`、身份上下文和 `Idempotency-Key`。
- MQ 事件包含 `event_id`、事件类型、事件版本、业务主键和发生时间；按至少一次投递设计，消费者必须幂等，并具备重试、死信和补偿路径。
- Java 数据库变更使用 Flyway 或 Liquibase，采用“先扩展、再迁移、最后删除”的兼容发布策略，不能让回滚依赖不可逆的结构删除。
- 商品结构化查询预留 OpenSearch/Elasticsearch 边界；原始数据、处理快照和向量索引备份预留 S3、OSS 或 MinIO 对象存储边界。

### 主 Agent 的分布式部署决策

主 Agent 在逻辑上保持一个 Supervisor，负责意图识别、任务规划、Agent 路由和结果汇总；在物理部署上，`v0.5.0` 起将 Agent Orchestrator 作为无状态服务部署多个副本。它不是只能运行在一台机器上的唯一总控节点：

```text
客户端 → API Gateway → Agent Orchestrator 多副本
                              ↓
                   Redis / PostgreSQL 外置状态
                              ↓
                      Queue + Worker Pool
                              ↓
                         MCP Gateway
                              ↓
                   Java Spring Cloud 多副本服务
```

LangGraph Checkpoint、会话、任务状态和幂等键不能只保存在某个 Supervisor 进程内，任意副本都必须能处理同一用户的后续请求。同一会话的并发消息需要通过会话版本号、队列化或分布式锁保证顺序。Product、Review、Recommend 等子 Agent 初期仍作为 Python 服务内部模块；长耗时和资源密集型任务由独立 Worker Pool 执行，并按 Worker 类型扩容。只有当某个 Agent 出现独立扩容、独立模型、故障隔离、独立发布或资源池隔离需求时，才拆成独立服务。

### 订单、库存与事件一致性决策

订单不能由 Agent 直接操作数据库，也不把“同步调用所有下游成功”当作跨服务事务。采用本地事务加事件最终一致性：

```text
用户确认 → Java 创建订单/库存预占
              ↓ 同一本地事务
         订单状态 + Outbox 事件
              ↓
       MQ 至少一次投递
              ↓
  支付、通知、索引、履约等幂等消费者
              ↓
     超时取消、库存释放和失败补偿
```

`v0.6.0` 必须确定订单状态机、库存预占/确认/释放、幂等键、唯一约束、Outbox 表和消费者幂等模型；`v0.7.0` 通过并发压测验证不超卖、不重复下单和消息恢复；不把分布式事务框架作为默认解决方案。

### Spring Cloud 组件职责

Java 业务层统一使用 Spring Cloud 体系，组件不是简单的技术名词清单：

| 组件 | 固定职责 |
|---|---|
| Spring Cloud Gateway | 统一入口、路由、鉴权前置、基础限流和请求追踪 |
| Nacos | 服务发现、实例健康和配置管理 |
| OpenFeign | Java 服务间声明式调用 |
| Spring Cloud LoadBalancer | 服务实例选择和负载均衡 |
| Sentinel | 限流、熔断、隔离、降级和热点保护 |
| Redis | 热点缓存、幂等键、短期状态和分布式协调 |
| MySQL | 订单、库存、用户等核心业务最终事实 |
| Kafka 或 RabbitMQ | 订单事件、库存事件、通知和索引更新 |

项目不同时引入 Sentinel、Resilience4j 等职责重叠的治理组件，除非后续有清晰的兼容性或专项需求。Spring Boot、Spring Cloud、Spring Cloud Alibaba、Nacos 和 JDK 版本在 `v0.6.0` 锁定兼容矩阵。

### 同步与异步任务边界

| 任务 | 执行方式 | 版本落点 |
|---|---|---|
| 普通商品推荐、商品详情、库存查询 | 同步接口 | `v0.1.0` 至 `v0.6.0` |
| 多商品深度对比、长文评价分析 | 异步任务 | `v0.5.0` |
| 数据集导入、Embedding 和向量索引构建 | 异步批处理 | `v0.3.0`、`v0.5.0` |
| 管理员评估任务 | 异步任务 | `v0.7.0` |

异步任务必须有 `task_id`、状态机、幂等键、超时时间、重试次数、错误码、结果引用和取消语义；不能把长耗时任务放在同步 Supervisor 请求中无限等待。

### 生产可靠性补充路线

- `v0.6.0`：确定连接池、线程池和队列上限，完成 Java 业务服务的基础健康检查、优雅下线、数据库迁移和核心契约测试。
- `v0.7.0`：完成 Python、MCP、Java 的统一 Trace 和指标，使用 Testcontainers 做跨服务集成测试，使用 k6 或 JMeter 完成商品读压、库存并发、订单幂等、消息积压和端到端混合压测。
- `v0.8.0`：完成就绪/存活探针分离、Redis Sentinel/Cluster 演进验证、MySQL 主从和备份恢复、Kafka 消费组与积压监控、故障注入、灰度/滚动发布和自动回滚。
- `v1.0.0`：完成 Prompt Injection 防护、敏感信息脱敏、API Key 加密存储与轮换、管理员审计、用户数据删除、灾备演练和生产 SLO。

## 分支与 CI/CD 流程

项目采用 Trunk-Based Development（主干开发）配合 CI/CD 敏捷交付。`main` 是唯一长期分支，功能通过短生命周期分支和 Pull Request 合并到 `main`；`dev`、`uat`、`prod` 是 CI/CD Environment，不是 Git 分支。

```text
需求 / Issue
    ↓
feature/*（短生命周期）
    ↓ Pull Request
CI：测试、构建、安全检查
    ↓ 合并
main（始终保持可构建、可部署）
    ↓ 同一份不可变 Docker 镜像逐级晋级
dev → uat → prod
```

### 分支规则

- `main` 受保护，必须通过 Pull Request、CI 检查和必要的代码审查后才能合并。
- `feature/*` 只承载一个清晰的功能、缺陷修复或技术任务，完成后及时删除。
- 版本使用 Git Tag、GitHub Release 和 Milestone 管理，例如 `v0.2.0`，不使用长期版本分支代替版本管理。
- 暂不创建长期 `develop`、`dev`、`uat`、`release` Git 分支，避免分支内容与部署环境逐渐偏离。
- 功能尚未完整时，通过 Feature Flag、配置开关或隐藏入口合并代码，保持 `main` 可发布。
- 线上紧急修复可以临时使用 `hotfix/*`，修复完成后必须合并回 `main`，不能形成长期分支。

### CI：合并前自动检查

每个 Pull Request 以及推送到 `main` 时执行 CI，至少包含：

- Python 依赖安装、单元测试和 API 测试。
- React + TypeScript 类型检查和生产构建。
- `docker compose config` 配置校验。
- API 和 Web Docker 镜像构建检查。
- 后续补充依赖漏洞扫描、敏感信息扫描和 Agent 质量回归。
- 测试失败时禁止合并到 `main`。

### CD：环境逐级晋级

合并到 `main` 后只构建一次 Docker 镜像，并使用 Git commit SHA 或版本号作为不可变标签。之后由同一份镜像逐级部署：

| CI/CD Environment | 发布方式 | 目标 |
|---|---|---|
| `dev` | 合并 `main` 后自动部署 | 快速集成和开发验证 |
| `uat` | 测试通过后人工批准 | 验收核心业务流程 |
| `prod` | UAT 通过后人工批准 | 生产发布、健康检查和回滚 |

不同环境的数据库、模型配置和密钥通过 GitHub Actions Environment Secret 或环境变量注入，不能写入仓库。当前先落地 PR CI；等部署目标和服务器确定后，再补充具体 CD 发布脚本，不提前绑定云厂商或基础设施。

### CI/CD 验收标准

- 任意 Pull Request 都能自动得到可追踪的测试结果。
- 通过同一份构建流程生成可复用的 Docker 镜像，避免“测试镜像”和“生产镜像”内容不一致。
- 开发环境、UAT 环境和生产环境使用不同配置和密钥，但部署步骤可复用。
- 发布失败时可以定位失败阶段，并回滚到上一个已验证版本。
- 每次发布都能关联 Git commit、镜像标签、测试结果和部署记录。

## 版本总览

| 版本 | 定位 | 核心结果 | 状态 |
|---|---|---|---|
| `v0.1.0` | 生产级 Agent 基线原型 | Docker/Compose、存活/就绪健康检查、外部配置、Mock LLM、Redis 会话和 Vite Web 技术验证端；`request_id`/`trace_id`、统一结构化错误、Docker 测试验收和 Mock API 压测基线已完成 | 基线验收已完成 |
| `v0.2.0` | 真实模型与稳定协议 | 可配置 LLM、结构化输出、模型降级、统一错误处理、身份凭证协议 | 进行中 |
| `v0.3.0` | Taro 用户 MVP、RAG 与 Python 长期记忆 | Taro 客户端、用户身份接入、开源数据集管道、商品知识、评价检索、RAG、历史会话和长期记忆 | 计划中 |
| `v0.4.0` | Agent 编排与动态运行时 | LangGraph、Supervisor、意图识别、规划路由、结果汇总、Agent Registry、动态 Agent 模板和工具白名单 | 计划中 |
| `v0.5.0` | 分布式 Agent、Worker 与横向扩容 | Agent API 多副本、Redis 外置状态、队列、Worker 池、异步任务、幂等、重试、恢复和负载均衡 | 计划中 |
| `v0.6.0` | Java 用户/交易服务、MCP 与高并发基线 | Java 无状态多副本、网关与服务发现、Redis 缓存、数据库读写边界、消息队列、库存并发控制、交易幂等、MCP 和确认下单闭环 | 计划中 |
| `v0.7.0` | 可观测、评估与管理员运营 | OpenTelemetry、Prometheus/Grafana、评估集、模型/API Key/Prompt 配置、成本分析和管理员后台 | 计划中 |
| `v0.8.0` | 高可用与生产发布 | 限流、熔断、自动扩容、故障注入、备份恢复、灰度/滚动发布、回滚和容量模型 | 计划中 |
| `v1.0.0` | 完整生产化平台 | 完整 RBAC、租户隔离、审计、密钥治理、生产 SLO、灾备、CI/CD 和完整文档 | 计划中 |

## 设计书能力覆盖

| 设计书能力 | 计划版本 | 版本落点 |
|---|---|---|
| Supervisor 与固定 Agent 编排 | `v0.1.0`、`v0.4.0` | 先完成顺序原型，再升级为可编排状态图 |
| 真实模型与 LLM Gateway | `v0.2.0`、`v0.7.0` | 先统一 Provider，再建设多模型路由、限流和成本治理 |
| 开源数据集与数据管道 | `v0.3.0` | 许可证核验、快照管理、清洗去重、字段映射、质量校验和可重建索引 |
| Taro、RAG 与长期记忆 | `v0.3.0` | 交付移动端用户 MVP、商品知识和 Python 侧记忆 |
| Dynamic Agent Runtime | `v0.4.0` | 模板、Registry、白名单工具和状态流转 |
| Worker、队列与异步任务 | `v0.5.0` | 任务拆分、并行执行、重试、恢复和状态查询 |
| 无状态服务与横向扩容 | `v0.1.0` 起步，`v0.5.0` 深化 | 先完成无状态约束，再落地多副本、外置状态、负载均衡和扩容压测 |
| MCP 与 Java 交易系统 | `v0.6.0` | 通过受控工具接入商品、库存、订单和支付能力 |
| Java 高并发分布式基线 | `v0.6.0`，`v0.8.0` 深化 | 多副本、网关负载均衡、缓存、数据库连接池/读写边界、消息队列、库存并发控制、限流熔断和压测；后续补自动扩容与灾备 |
| Agent Trace 与 Evaluation | `v0.7.0` | 建立链路追踪、质量评估、成本分析和回归门禁 |
| 管理员、API Key 与平台治理 | `v0.7.0`、`v1.0.0` | 先做 Agent 管理能力，再完善企业级 RBAC、审计和密钥治理 |
| 生产可靠性与 CI/CD | `v0.1.0` 起步，`v0.8.0`、`v1.0.0` 完整 | 先有 CI 和 Compose，逐步完成同镜像晋级、回滚、高可用和灾备 |
| 部署与环境治理 | `v0.1.0` 起步，`v0.8.0`、`v1.0.0` 完整 | 本地 Compose、多副本集成环境、dev/uat/prod 晋级和生产运维闭环 |
| 压测与容量模型 | `v0.1.0` 起步，`v0.5.0`、`v0.8.0` 深化 | 建立基线、扩容验证、故障压测、长稳压测和容量预测 |

---

## `v0.1.0`：生产级 Agent 基线原型

**目标**：建立一个不依赖真实大模型也能运行的生产级约束基线，验证演示客户端、Agent 后端、商品数据和会话存储之间的边界；它不是最终面向用户发布的客户端 MVP。

### 核心功能

- React + TypeScript + Vite 智能购物技术验证与接口演示客户端。
- FastAPI 聊天接口、健康检查接口和请求/响应模型。
- Supervisor → Product Agent → Recommend Agent 的基础顺序编排。
- 按预算、品类和关键词筛选商品，并返回推荐理由。
- Redis 保存会话消息，支持基于 `session_id` 的多轮交互。
- Mock 推荐模式，不配置模型密钥也能运行。
- Web、API、Redis 的 Docker Compose 一键启动。
- 后端单元/API 测试和前端构建检查。
- API 无状态约束、存活/就绪健康检查、`request_id`/`trace_id` 链路标识、统一结构化错误和外部配置基线。
- Docker 后端测试、Node 20 前端构建验收，以及 Mock LLM API 基准压测（10 并发、200 请求基线）。

### 验收标准

- 执行 `docker compose up --build` 后，前端、API 和 Redis 正常启动，API 健康状态为 `healthy`。
- 用户可以在页面输入购物需求并看到推荐商品卡片。
- `/health/live` 不依赖 Redis，`/health/ready` 能明确识别 Redis 状态。
- 所有响应包含 `X-Request-ID`/`X-Trace-ID`，错误使用统一结构化格式。
- `docker compose --profile test run --rm api-test` 和 `docker compose build web` 通过。
- Mock 模式压测稳定可重复，基线记录在 `benchmark/results/`。

### 暂不做

- 真实大模型调用、开源大规模数据集、向量数据库、订单支付和用户登录。
- 完整用户注册登录、管理员后台和生产级权限系统；当前仅使用匿名 `session_id`。
- 动态生成可执行代码的 Agent。

**状态：`v0.1.0` 生产级工程基线验收完成，后续版本在 Mock 回归和压测基线上继续演进。**

## `v0.2.0`：真实模型与稳定协议

**目标**：在不破坏 Mock 回归能力的前提下接入真实大模型，让模型调用成为可替换的基础设施，而不是散落在业务代码中的厂商 SDK。

### 核心功能

- 定义统一的 LLM Provider 接口和配置项，默认使用 Chat Completions，同时支持切换到 Responses API。
- 支持至少一个真实模型 Provider，并保留 Mock Provider。
- 结构化输出：意图、预算、品类、筛选条件和推荐结果使用明确的数据模型。
- 模型超时、有限重试、错误分类和基础降级。
- 模型密钥、模型名称和调用参数通过环境变量注入。
- 限制上下文长度，记录基础 Token 使用量和响应耗时。
- 定义 Java/宿主平台向 Agent 传递的身份凭证和身份上下文，包括 `user_id`、`tenant_id`、`role`、`scopes`、`session_id` 和 `trace_id`。
- 约定 Agent 侧会话历史、长期记忆和用户偏好的读写接口；Java 只负责签发和校验身份，不复制 Agent 记忆数据。
- 增加真实模型适配器的 Mock 测试和接口契约测试。

### 验收标准

- 通过配置即可在 Mock 和真实模型之间切换。
- 模型返回格式错误时，API 返回可理解的错误或降级结果，不泄露内部异常。
- 商品名称、价格和库存等事实只能来自检索结果，不能由模型自由编造。
- 无模型密钥时，Docker 启动和现有回归测试仍然通过。

### 暂不做

- 复杂 RAG、多模型自动路由和多租户计费。
- 让大模型直接执行数据库写操作。

**状态：进行中。** LLM Provider、DeepSeek Chat/Responses 适配、Mock 默认模式、超时/有限重试和失败降级已完成；管理员配置台第一版已完成运行期草稿、脱敏读取、连接测试和启用切换。结构化意图输出、身份上下文、Token/耗时统计、完整 Provider 契约、加密持久化、管理员账号/RBAC、审计和多副本配置同步仍待完成。当前配置台状态仅保存在单个 API 进程内，不作为生产级配置中心使用。

## `v0.3.0`：面向用户的 MVP 与商品知识

**目标**：交付可实际演示的移动端优先用户 MVP，让官方客户端适合移动购物场景，并让推荐不仅有结果，还有可追溯的商品参数和用户评价依据。

### 核心功能

- 使用 Taro + React + TypeScript 重构官方用户端，采用移动端优先的页面和交互设计。
- 支持 H5 以及多个小程序平台的跨端构建目标，复用接口、数据模型、业务逻辑和大部分页面组件。
- 通过统一的 API 协议接入其他 App、商城或企业应用；当前只建设构建适配能力，不单独承诺某个小程序平台的上线发布。
- 完善商品、评价、分类和属性数据模型。
- 商品与评价数据导入、清洗和可重复构建流程。
- 接入经过许可证核验的开源商品和评价数据集，固定数据快照版本并建立数据集清单。
- 建立原始数据下载、格式转换、去重、字段标准化、质量校验、脱敏和失败重跑流程；原始数据不提交 Git。
- 文档切分、Embedding、向量存储和召回链路。
- 商品检索与评价检索结合，支持关键词过滤和语义召回。
- 基础 Reranker 或规则重排，降低无关评价进入上下文的概率。
- Review Agent，输出产品优缺点、使用场景和评价摘要。
- 推荐结果显示依据，支持查看关联商品参数或评价片段。
- 商品详情、候选对比和历史会话查看能力。
- 接入 Java 或宿主平台的登录态；普通用户可以查看自己的历史会话、偏好和记忆，不能访问其他用户数据。
- 在 Python Agent 侧落地长期记忆：会话消息、历史摘要、用户偏好、用户画像和可检索的语义记忆。
- Taro 客户端只通过 Agent API 访问历史和记忆，不直接访问 Redis、向量数据库或记忆存储。

### 验收标准

- 给定测试问题，可以检索出相关商品和评价证据。
- 推荐理由能够关联到检索到的事实，不能只返回无依据的营销话术。
- 通过离线样例集验证 Recall@K、引用覆盖率和回答一致性。
- 数据导入可重复执行，并能识别重复数据和失效数据。
- 数据集清单能够追溯来源、许可证、版本、下载时间、处理脚本和校验摘要；从同一快照可以重建结构化数据和向量索引。
- 开源数据集中的许可和隐私风险经过人工核验，生产部署不依赖未审核的在线下载地址。
- 使用同一用户身份在不同设备或客户端恢复自己的历史会话和长期记忆。
- 记忆写入、查询和删除都按 `user_id`、`tenant_id`、`session_id` 做隔离校验。

### 暂不做

- 实时库存和价格同步。
- 复杂个性化画像和自动读取用户隐私数据。
- 特定小程序平台的主体认证、经营资质、支付签约和正式上线运营。
- 超大规模实时商品数据同步和在线推荐特征平台。

**状态：计划中。**

## `v0.4.0`：Agent 编排与动态运行时

**目标**：将当前顺序调用升级为可观察、可扩展、可恢复的 Agent 工作流。

### 核心功能

- 使用 LangGraph 固化 Supervisor、Intent Classifier、Planner、Router 和 Result Aggregator 的状态流转。
- 意图识别在代码和模块层面独立；初期作为 Supervisor 内部节点运行，不单独部署为微服务。
- 当意图识别出现独立扩容、独立模型、故障隔离或独立发布需求时，再拆出 Intent Service。
- 使用 Agent Registry 管理固定 Agent、动态 Agent 模板、版本和能力描述。
- 明确 Agent 状态模型、节点输入输出、终止条件和错误分支。
- 模板化动态 Agent：角色、目标、工具和提示词可配置，但不允许运行时生成任意代码。
- 工具注册、参数校验、权限 Scope 和工具白名单机制。
- 对复杂对比和研究任务进行任务拆分、并行执行和结果汇总。
- 基础 Agent 执行事件模型，为后续 Trace 和评估提供数据。
- 明确短期上下文、长期对话历史、用户偏好、用户画像和向量记忆的生命周期与清理策略。
- 普通用户只能操作自己的记忆；管理员可以按权限查看或管理 Agent 运行数据，但不能默认读取所有用户的原始隐私内容。

### 验收标准

- 新增一个固定 Agent 或工具时，不需要修改 Supervisor 的所有分支代码。
- 节点失败后可以明确重试、降级或终止，不出现无限循环。
- Supervisor 可以根据结构化意图选择固定 Agent 或模板 Agent，完成规划、路由和结果汇总。
- 长耗时任务可以生成任务描述和状态事件，为下一版本 Worker 异步执行做准备。
- 动态 Agent 只能使用白名单工具和模板配置。

### 暂不做

- 任意代码生成与执行。
- 没有权限边界的自动下单或自动支付。

**状态：计划中。**

## `v0.5.0`：分布式 Agent、Worker 与横向扩容

**目标**：将 Agent 运行时从单进程验证提升为可横向扩展的分布式执行平台。

### 核心功能

- Agent API 多副本部署，进程不保存会话、记忆和任务状态，任意副本都可处理同一会话请求。
- Redis 外置会话、任务状态、幂等键和必要的短期运行数据；持久化数据与缓存分层管理。
- Queue 与 Worker Pool，至少划分 Product、Review、Recommend 和 Research Worker。
- 长耗时任务异步化，提供任务创建、查询、进度事件、结果读取和取消语义。
- 任务幂等、有限重试、指数退避、超时、死信或失败任务记录，以及 Worker 重启后的恢复策略。
- 服务发现、负载均衡和优雅下线；为 API 和 Worker 分别设置扩容指标。
- 建立 API、队列、Worker、Redis 和向量检索的多副本压测场景。

### 验收标准

- 任意 Agent API 副本都能读取同一会话并返回一致的任务状态。
- 重复投递不会重复执行任务，Worker 故障后任务可重试或进入可追踪的失败状态。
- 队列堆积、消费延迟、吞吐量和扩容拐点可以通过压测数据说明。
- 多副本部署、负载均衡和优雅下线不丢失请求或任务状态。

### 暂不做

- Java 交易服务、真实支付渠道和完整履约系统。
- 将订单数据库直接暴露给 Agent 或大模型。

**状态：计划中。**

## `v0.6.0`：Java 用户/交易服务、MCP 与高并发基线

**目标**：把 Agent 能力安全接入 Java 用户和交易业务，并在第一版 Java 业务服务中建立能够承受高并发压测的分布式基线，避免端到端压测时 Python Agent 正常而 Java 业务层成为单点瓶颈。

### 核心功能

- Java 17 + Spring Boot 业务服务基础骨架，至少包含用户、商品、库存、购物车和订单边界。
- Java API 服务无状态化，支持多副本部署；网关负责统一入口、鉴权前置、路由、负载均衡和基础限流。
- 使用 Nacos 或等价服务发现能力管理服务实例，禁止业务服务依赖固定 IP 和单实例地址。
- Redis 缓存热点商品、分类和可安全缓存的查询结果，明确缓存过期、主动失效、穿透、击穿和雪崩防护策略。
- MySQL 事务写入保持强一致，配置连接池、索引和慢查询治理；商品等读多写少数据预留或落地读写分离，订单和库存写入固定主库。
- 使用 Kafka/RabbitMQ 等消息队列承载订单事件、通知、搜索索引更新等非核心同步操作，避免请求线程串行等待所有下游服务。
- 库存使用原子扣减、乐观锁或库存预占机制防止超卖；订单使用幂等键、唯一约束和状态机防止重复下单。
- 为 Java 服务配置超时、有限重试、熔断、隔离、降级和优雅下线，避免级联故障拖垮整个链路。
- MCP Client、MCP Gateway 和 Java MCP Server 的受控调用链路。
- 商品查询、商品详情、库存检查、创建订单、查询订单等白名单工具。
- 用户明确确认后才能进入创建订单流程；Java 服务执行最终权限、库存和业务校验。
- 订单幂等、库存校验、订单状态机和基础事务处理。
- Python Agent 使用 Java 传递的身份上下文访问记忆和会话，不重复实现账号登录。
- 基础指标覆盖 QPS、并发连接、P95/P99、线程池、连接池、缓存命中率、数据库慢查询、队列堆积、库存冲突和错误率。
- 建立 Java 业务层独立压测和 Agent → MCP → Java 端到端压测，先测读流量，再测库存/订单写流量和混合流量。

### 验收标准

- Agent 无法绕过确认步骤直接创建订单，重复请求不会产生重复订单。
- 库存不足、工具超时、权限不足和业务校验失败都有明确错误结果。
- 商品事实、库存结果和订单状态以 Java 业务服务返回为准。
- Java API 多副本可以通过网关均衡处理请求，任一实例重启不丢失会话、订单和任务状态。
- 热点商品查询优先命中缓存；缓存失效、数据库连接池耗尽、消息队列堆积和下游超时都有可观察的保护或降级结果。
- 高并发库存请求不会超卖，重复订单请求不会产生重复订单；压测报告能够说明 Java 服务、数据库、缓存和队列的瓶颈位置。
- Java 服务的压测结果必须单独记录吞吐量、P50/P95/P99、错误率、数据库连接池、缓存命中率、队列延迟和资源使用率，并纳入端到端容量模型。

**状态：计划中。**

## `v0.7.0`：可观测、评估与管理员运营

**目标**：能够回答“系统为什么这样回答、哪里变慢了、质量是否变好了、一次请求花了多少成本”。

### 核心功能

- 使用 OpenTelemetry 记录请求、Agent、Tool、LLM 和数据检索 Trace。
- 使用 Prometheus/Grafana 或等价方案记录吞吐、错误率、P95 延迟和依赖状态。
- 构建购物意图、商品筛选、评价分析和交易确认评估集，自动评估意图准确率、Recall@K、Faithfulness、工具成功率和任务成功率。
- 记录 Token 消耗、模型耗时、单次请求成本和按租户的用量。
- 将评估结果接入 CI，阻止关键质量指标明显回退。
- 管理员查看脱敏 Trace、模型调用量、成本、错误和质量评估；普通用户只能查看自己的请求状态和结果。
- 模型 Provider、API Key、Prompt 和 Agent 运行配置属于管理员能力，完整 API Key 不返回客户端，使用加密存储或 Secret Manager。

### 验收标准

- 可以通过 `trace_id` 还原一次请求的完整 Agent 调用链，并定位具体失败节点。
- 每次 Prompt、模型或检索策略变更都有可比较的评估结果。
- 监控和观测数据不包含密钥、用户隐私和未脱敏的敏感内容。

**状态：计划中。**

## `v0.8.0`：高可用与生产发布

**目标**：在已有分布式运行时上补齐生产可靠性、发布安全和容量验证。

### 核心功能

- API Gateway、限流、熔断、超时、有限重试、服务级降级和过载保护。
- Agent API、Worker 和关键依赖的自动扩容策略与资源上限。
- 数据库、Redis、消息队列、向量索引和配置的备份恢复流程。
- dev → uat → prod 逐级晋级，同一份不可变镜像执行灰度、滚动发布、健康检查和回滚。
- 故障注入：LLM 超时、Redis 不可用、队列堆积、Worker 崩溃、向量库异常和网络延迟。
- 稳定性压测、峰值压测、长稳压测和容量模型，记录扩容拐点与资源预算。

### 验收标准

- 依赖异常时按预期限流、熔断或降级，恢复后服务可自动恢复。
- 发布失败可以停止晋级并回滚到上一个已验证镜像。
- 备份可以恢复，故障演练结果和未解决风险有记录。
- 压测结果能够支撑副本数、Worker 数量和关键资源的容量估算。

**状态：计划中。**

## `v1.0.0`：生产化智能购物平台

**目标**：形成一个边界清晰、可部署、可维护、可演示和可持续迭代的企业级智能购物平台。

### 核心功能

- Java 统一提供用户认证、令牌签发、角色/租户管理和业务授权；Agent 校验凭证并执行自身资源的二次授权。
- 至少区分普通用户和管理员：普通用户只能访问自己的会话、长期记忆和推荐数据；管理员才可以管理模型配置、API Key、Prompt、Agent 工具和观测数据。
- 完整 RBAC、租户隔离、资源级权限、审计日志、敏感操作二次确认、密钥轮换和用户数据删除流程。
- API Gateway、限流、熔断、超时、重试和服务级降级。
- 商品数据、向量索引、会话和订单的备份与恢复策略。
- 生产环境配置、镜像发布、CI/CD 和数据库迁移流程。
- 开发环境、UAT 环境和生产环境的自动部署、审批、健康检查和回滚流程。
- 前端错误状态、加载状态、流式响应和移动端适配完善。
- 核心 API、Agent 工作流、工具调用、数据导入和前端交互的自动化测试。
- 安全审计、依赖漏洞扫描、日志脱敏和 Prompt Injection 防护。
- 面向开发者的架构文档、部署文档、API 文档和故障排查手册。

### 验收标准

- 新环境按照部署文档可以完成启动、初始化、健康检查和回滚。
- 关键链路有明确的 SLO、监控告警和故障处理方式。
- 未授权用户不能访问其他用户的会话、订单或个人数据。
- 未授权用户不能访问其他用户的 Agent 长期记忆；管理员的观测权限默认脱敏、只读和按租户隔离。
- 用户删除或撤回授权后，Python 侧的历史会话、画像、偏好、向量记忆和缓存可以按策略清理并留下审计结果。
- 版本发布具备可追踪的变更记录、测试结果和回滚方案。
- 同一份不可变 Docker 镜像经过开发环境和 UAT 验证后再发布生产，避免环境间构建内容不一致。

**状态：计划中。**

## 建议开发顺序

当前应优先推进 `v0.2.0`，顺序建议如下：

1. 完成 `v0.2.0` 的 LLM Provider、结构化输出、错误模型、超时重试和身份协议。
2. 接入真实模型，保留 Mock 作为默认回归后端，并完成模型调用基线压测。
3. 在 `v0.3.0` 接入经过许可证核验的开源商品/评价数据集，完成可重建数据管道、RAG、长期记忆和 Taro MVP。
4. 在 `v0.4.0` 引入 LangGraph、Supervisor、意图识别、规划路由、动态 Agent 和工具权限控制。
5. 在 `v0.5.0` 落地 API 多副本、外置状态、队列、Worker Pool、任务恢复和横向扩容压测。
6. 在 `v0.6.0` 接入 MCP 与 Java 用户/交易服务，同时完成 Java 无状态多副本、缓存、数据库连接池/读写边界、消息队列、库存并发控制和高并发基线压测。
7. 在 `v0.7.0` 建设覆盖 Python、MCP 和 Java 的统一观测、评估、成本治理和质量回归门禁。
8. 在 `v0.8.0` 基于压测结果完成 Java 与 Agent 的自动扩容、限流熔断、故障演练、备份恢复、容量模型和生产发布；再以 `v1.0.0` 收敛完整治理能力。

## 版本边界

本路线图只描述公开的产品能力和技术方向，不记录个人学习进度、私人设计资料、密钥、环境信息和临时实验结论。这些内容继续保存在本地 `docs-private/` 中。
